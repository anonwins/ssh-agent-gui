namespace SshAgentGui;

internal static class AutoUnloadUi
{
    public const string ComboToolTip =
        "Applies to the next key you load. Change a loaded key from its countdown. " +
        "This app unloads the key while it is running; Windows ssh-agent does not.";
}
